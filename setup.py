from setuptools import setup, find_packages

setup(
    name='vqgan_jax',
    version='0.0.1',
    description='blabla',
    packages=find_packages(),
    install_requires=[
        'flax',
        'transformers',
    ],
)